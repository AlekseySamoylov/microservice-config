eureka:
  instance:
    metadataMap:
      region: Canada
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
server:
  port: 8081

spring:
  boot:
    admin:
      url: http://localhost:8086
  profiles:
    active: production
  application:
    name: client-app-usa

management:
  security:
    enabled: false

