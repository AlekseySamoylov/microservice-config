eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka/
    register-with-eureka: false

server:
  port: 8765

zuul:
  prefix: /api
  routes:
    client:
      path: /client/**
      serviceId: client-service
